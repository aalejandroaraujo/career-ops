# assets/

User-layer static assets for CV generation.

- `headshot.jpg` (or .jpeg/.png) — your CV photo, used by the AltaCV template (templates/cv-altacv.tex).
  Square-ish, head & shoulders. The build copies it next to the generated .tex at compile time.
